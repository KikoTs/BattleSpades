# Development checkpoint for Mac handoff

Work and tests were stopped at the user's request on 2026-09-21. This is an
in-progress checkpoint, not an approved beta release. Pull `main` in both
`KikoTs/BattleSpades` and `KikoTs/BattleSpadesClient` before continuing.

The checkpoint includes the gameplay, scoring/voting, flight, deployable,
greeting, and cooperative bot changes from this session. The latest bot pass
adds committed combat movement, bounded gaze, mode-aware VIP roles, and carrier
route and weapon-permission resets. See the dated `BOT_*` notes for scope and
limitations rather than assuming every requested issue is accepted.

Focused suites passed before stopping: 151 movement/tactics/mode/navigation
checks, 55 gaze/carrier-permission checks, and 43 trace-review checks. Carrier
mobility regressions also passed. These are separate suite results, not a
deduplicated total. The final native VIP Alcatraz, VIP Chicago, and CastleWars
CTF runs were interrupted around 58 seconds and are not accepted. All owned
test processes were stopped. See `BOT_NATURAL_MODE_VALIDATION_2026-09-21.md`.

Next validation should exercise local native matches, including flag pickup,
return routing, rejected carrier excavation, VIP death/round transitions, and
continuous observation of movement/head rotation. Earlier CTF traces exposed a
stale excavation loop; the latest source fixes it, but the final full-match
rerun was not completed. No completed CTF capture was demonstrated in the
completed candidate runs. Do not test against the public legacy server.

The Windows client's existing `dist/bin/server` contains the preceding
candidate, not the latest carrier fixes. Rebuild from source on the target
platform using the repository build documentation. Local `out/` traces, build
outputs, generated credentials, and runtime state are not committed. Client
retail assets remain local imports as described in its asset documentation.
Preserve the normal account/settings and use Create Match for the eventual
playtest. Release publication remains pending the user's approval.

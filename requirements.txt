# BattleSpades server dependencies.
#   pip install -r requirements.txt
#
# NOTE: the vendored ENet/pyenet binding and Cython extensions compile from
# source, so a C/C++ toolchain is required (see README -> Building from source).

# The reliable-UDP ENet binding is built by setup.py from vendor/pyenet.

# Python 3.12 virtual environments no longer install setuptools implicitly.
setuptools>=75

# Cython — builds the compiled physics / VXL / packet core.
Cython>=3.0.0

# Config file parsing.
toml>=0.10.2

# Inspectable behavior-tree composition inside the isolated bot worker.
py_trees>=2.5,<2.6

# --- development / testing (optional for just running the server) ---
pytest>=7.0.0
pytest-asyncio>=0.20.0

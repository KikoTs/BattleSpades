# BattleSpades server dependencies.
#   pip install -r requirements.txt
#
# NOTE: pyenet and the Cython extensions compile from source, so a C toolchain
# is required (see README -> Building from source).

# Reliable-UDP networking (ENet binding) — the game transport.
pyenet>=1.3.17

# Cython — builds the compiled physics / VXL / packet core.
Cython>=3.0.0

# Config file parsing.
toml>=0.10.2

# --- development / testing (optional for just running the server) ---
pytest>=7.0.0
pytest-asyncio>=0.20.0
